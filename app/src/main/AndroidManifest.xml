<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yonatan.asusx541u.pacPrayerTime">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="com.yonatan.asusx541u.pacPrayerTime.AppSorek"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".presenter.ImagePopUp"></activity>
        <activity android:name=".presenter.NewsActivity" />
        <activity android:name=".presenter.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.AboutActivity" />
        <activity android:name=".presenter.MainActivity" />
        <activity android:name=".presenter.PrayersActivity" />
        <activity android:name=".presenter.BusinessActivity" />
        <activity android:name=".presenter.StoresActivity" />
        <activity
            android:name=".presenter.PopActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".presenter.LessonsActivity" />
        <receiver android:name=".broadcastReceiver.PrayerAlertReceiver"
            android:enabled="true"
            android:exported="false"/>
        <service android:name=".services.SorekFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>